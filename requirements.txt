fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1

sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1

redis[hiredis]==5.0.4

qdrant-client==1.9.1
fastembed>=0.3.6          # local embedding model (BAAI/bge-small-en-v1.5, 384-dim)

anthropic==0.27.0

langgraph==0.1.2

checkov>=3.2.529          # installed separately in Dockerfile too; pinned here for local dev
# trufflehog and semgrep are called as external binaries via subprocess

PyGithub==2.3.0

kubernetes==29.0.0
kubernetes-asyncio==24.2.3

structlog==24.1.0
python-dotenv==1.0.1

pytest==8.2.0
pytest-asyncio==0.23.7
httpx==0.27.0            # async test client for FastAPI

daytona>=0.187.0
